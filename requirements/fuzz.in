# Runtime dependencies for the ClusterFuzzLite fuzz build (.clusterfuzzlite).
# The local camt053_lsp package is added to PyInstaller's --paths instead of
# being pip-installed, so no un-pinnable local install is needed.
pygls>=1.3
camt053>=0.0.6,<1
