# Lint/type-check tooling for the CI "lint" job.
# Runtime deps (pygls, camt053) are included so mypy --strict can resolve the
# pygls decorators rather than treating them as untyped.
pygls>=1.3
camt053>=0.0.6,<1
ruff==0.15.22
black==26.5.1
mypy>=1.11,<3.0
